	switch (t->back) {
	default: Uerror("bad return move");
	case  0: goto R999; /* nothing to undo */

		 /* CLAIM SEM6_cond0 */
;
		;
		;
		;
		;
		;
		;
		;
		
	case 7: // STATE 20
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* CLAIM SEM4_var0 */
;
		
	case 8: // STATE 1
		goto R999;

	case 9: // STATE 10
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* CLAIM SEM3_1_var0 */
;
		
	case 10: // STATE 1
		goto R999;

	case 11: // STATE 10
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC main */

	case 12: // STATE 1
		;
		cond0 = trpt->bup.oval;
		;
		goto R999;

	case 13: // STATE 2
		;
		cond0 = trpt->bup.oval;
		;
		goto R999;

	case 14: // STATE 5
		;
		isdef_var0 = trpt->bup.oval;
		;
		goto R999;

	case 15: // STATE 7
		;
		now.init_count_var0 = trpt->bup.oval;
		;
		goto R999;

	case 16: // STATE 8
		;
		now.init_count_var0 = trpt->bup.oval;
		;
		goto R999;

	case 17: // STATE 10
		;
		now.true_count_cond0 = trpt->bup.oval;
		;
		goto R999;

	case 18: // STATE 11
		;
		now.use_count_var0 = trpt->bup.oval;
		;
		goto R999;

	case 19: // STATE 15
		;
		now.true_count_cond0 = trpt->bup.oval;
		;
		goto R999;

	case 20: // STATE 16
		;
		now.use_count_var0 = trpt->bup.oval;
		;
		goto R999;

	case 21: // STATE 19
		;
		isdef_var0 = trpt->bup.oval;
		;
		goto R999;

	case 22: // STATE 20
		;
		now.use_count_var0 = trpt->bup.oval;
		;
		goto R999;

	case 23: // STATE 21
		;
		now.init_count_var0 = trpt->bup.oval;
		;
		goto R999;

	case 24: // STATE 22
		;
		destroy_count_var0 = trpt->bup.oval;
		;
		goto R999;

	case 25: // STATE 23
		;
		p_restor(II);
		;
		;
		goto R999;
	}

